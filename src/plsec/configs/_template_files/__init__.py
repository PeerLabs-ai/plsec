"""Canonical template files for plsec.

This package contains all template files used by both the plsec CLI
(via importlib.resources) and the bootstrap assembler (via filesystem).

Templates are loaded by src/plsec/configs/templates.py which provides
the public Python API.
"""
